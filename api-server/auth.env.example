# Copy values into repo-root .env or api-server/.env

COGNITO_REGION=ap-south-1
COGNITO_USER_POOL_ID=ap-south-1_RPHo5WjDk
COGNITO_CLIENT_ID=74i9tr52i2v3c1v3pceq3as6e3

# Optional: dedicated Cognito IAM user (otherwise uses default AWS credential chain / CLI profile)
# COGNITO_AWS_ACCESS_KEY_ID=
# COGNITO_AWS_SECRET_ACCESS_KEY=

# Required for custom-token bridge (recommended production path)
# FIREBASE_SERVICE_ACCOUNT_KEY=./serviceAccountKey.json

PORT=8080
